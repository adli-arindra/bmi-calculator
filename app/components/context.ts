"use client";
import { createContext } from "react";

export const foodContext = createContext<any | undefined>(undefined);

export const personContext = createContext<any | undefined>(undefined);
